import "../objects/GuideLine"
import Handler from "./Handler"

class GuideLineHandler {
  handler: Handler

  constructor(handler: Handler) {
    this.handler = handler
  }
}

export default GuideLineHandler
